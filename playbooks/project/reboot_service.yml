- name: Forzar reinicio de servicio crítico caído
  hosts: "{{ target_host | default('localhost') }}"
  gather_facts: no
  tasks:
    - name: Reiniciar servicio afectado de forma real
      ansible.builtin.systemd:
        name: "{{ service_name | default('nginx') }}"
        state: restarted
      become: yes