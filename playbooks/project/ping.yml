- name: Verify Ansible connectivity to an approved target
  hosts: managed_target
  gather_facts: false
  tasks:
    - name: Verify Python execution through Ansible
      ansible.builtin.ping:
      changed_when: false
